import { Meta, Typeset } from '@storybook/addon-docs/blocks';
const fontSizes = [
    'var(--scale-00)',
    'var(--scale-0)',
    'var(--scale-1)',
    'var(--scale-2)',
    'var(--scale-3)',
    'var(--scale-4)',
    'var(--scale-5)'
  ],
  sampleText = `<%= description %>`;

<Meta title="Brand/Typography" />

# Typography

<br />

## Sans Serif

#### Regular

<Typeset {...{ fontSizes, sampleText }} />
