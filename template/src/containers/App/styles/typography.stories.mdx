import { Meta, Typeset } from '@storybook/addon-docs/blocks';
const fontSizes = [12, 14, 16, 18, 20, 24, 30, 36, 48, 80, 96],
  fontFamily = 'sans-serif',
  sampleText = `<%= description %>`;

<Meta title="Brand/Typography" />

# Typography

<br />

## Sans Serif

Sans serif is pretty boring, but it's the default we're using

### Regular

<Typeset fontWeight={400} {...{ fontFamily, fontSizes, sampleText }} />
